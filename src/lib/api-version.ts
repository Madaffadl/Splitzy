// The current API contract version. Advertised on every /api/* response via the
// X-API-Version header (see next.config.mjs) and available to code that needs to
// branch on or echo the version. See docs/API_VERSIONING.md.
export const API_VERSION = "1";
